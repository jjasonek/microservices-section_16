apiVersion: v1
kind: Service
metadata:
  name: {{ include "eazybank-common.fullname" . }}
  labels:
    {{- include "eazybank-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eazybank-common.selectorLabels" . | nindent 4 }}
