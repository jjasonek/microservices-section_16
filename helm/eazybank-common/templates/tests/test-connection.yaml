apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eazybank-common.fullname" . }}-test-connection"
  labels:
    {{- include "eazybank-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eazybank-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
